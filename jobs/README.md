Copy the exported jobs as XML files here, and list them in the playbook as

    jenkins_jobs:
      - job-file-name-without-extension

The jobs will be named `job-file-name-without-extension` in Jenkins.